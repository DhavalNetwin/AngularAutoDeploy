pipeline {
  agent none
  stages {
    stage('checkout'){
      steps{
        checkout scm
      }
    }

    stage('Install') {
      sh "npm install"
    }
    
    stage('Build') {
      sh "npm run build --prod"
    }
  }
}