pipeline {
  agent any
  stages {
    stage('checkout'){
      steps{
        checkout scm
      }
    }

    stage('Install') {
      steps{
        sh "npm install"
      }
    }
    
    stage('Build') {
      steps{
        sh "npm run build --prod"
      }
    }
  }
}