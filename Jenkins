node {
  stage('Checkout') {
    git branch: 'Master', url: 'https://github.com/DhavalNetwin/AngularAutoDeploy.git' }

  stage('Node install') {
      sh "npm install" 
    }
	
	stage('Build') {
      sh "npm build"
  }
  
}