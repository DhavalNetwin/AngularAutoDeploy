pipeline {
  agent none
  stages {
    stage('checkout'){
      git url: 'https://github.com/DhavalNetwin/AngularAutoDeploy.git'
    }

      stage('Install') {
      sh "npm install"
      }
    
    stage('Build') {
      sh "npm run build --prod"
      }
  }
}