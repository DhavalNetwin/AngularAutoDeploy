pipeline {
  agent none
  stages {
    stage('checkout'){
      checkout scm
    }

    stage('Install') {
      sh "npm install"
    }
    
    stage('Build') {
      sh "npm run build --prod"
    }
  }
}