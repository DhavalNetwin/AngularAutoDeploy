node {
  stage('Checkout') {
    git branch: 'Main', url: 'https://github.com/DhavalNetwin/AngularAutoDeploy.git' }

  stage('Node install') {
      sh "npm install" 
    }
	
	stage('Build') {
      sh "npm build"
  }
  
}